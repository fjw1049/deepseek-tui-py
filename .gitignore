# Config with secrets
config.toml

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
.venv/
venv/
env/
build/
dist/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.worktrees/

# State / local data
~/.deepseek_tui/
*.db

# Project-local deepseek state (config.toml, sessions, tasks, logs, skills,
# state.db, secrets). Each clone keeps its own; nothing here should be
# committed.
.deepseek/

# IDE
.vscode/
.idea/
*.swp
.DS_Store
scratch/*

# Local debug session dumps
session_*.json

# Claude Code local settings (do not commit tool-permission files)
.claude/settings.local.json

# But keep the parity-reference Rust source tree clean of its stale IDE/session files
docs/DeepSeek-TUI-main/.idea/
docs/DeepSeek-TUI-main/.claude/
.claude/
