/** Workbench product feature gates (Stage 8). */

export const WORKBENCH_FEATURES = {
  /** Plugin marketplace UI — backed by /v1/plugins/registry. Off until a catalog ships. */
  pluginMarketplace: false
} as const
