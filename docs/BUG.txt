========================================================================
DeepSeek-TUI Python Rewrite — Work & Issue Tracker (BUG.txt)
========================================================================

This file tracks work done, bugs found, and deferred items during
the refactoring process. It is the companion to HANDOVER.md.

Convention:
  [DONE]     — completed
  [BUG]      — bug found, needs fix
  [DEFERRED] — intentionally deferred, with reason
  [ISSUE]    — environmental / tooling issue

========================================================================
2026-05-08  Stage 2 Integration Commit
========================================================================

Goal: Wire 3 orphan Engine modules (tool_parser, compaction, capacity)
into the runtime + fix command_safety dead code. These modules had code
but were never called, violating Principle B (no island code).

--- Work Log ---

[DONE] Step 0: Created docs/BUG.txt tracking file.

[DONE] Step 1: Wired tool_parser into turn_loop.py (text fallback).
  - Added import of has_tool_call_markers, parse_tool_calls to turn_loop.py
  - After StreamDone, if no structured tool_calls and buffer has text with
    tool markers, parse_tool_calls is called and results converted to ToolCall
  - ParsedToolCall.args -> ToolCall.arguments mapping implemented
  - ~15 LOC added to _run_turn_loop

[DONE] Step 2: Wired compaction into Engine + turn_loop (emergency).
  - Engine.__init__: added compaction_config (CompactionConfig), passed
    self._emergency_compact as compact_fn to TurnLoop
  - Engine._run_conversation: added should_compact() check before each LLM
    call; if triggered, calls compact_messages_safe() and replaces messages
  - Engine._emergency_compact: new method for TurnLoop callback
  - TurnLoop.__init__: added compact_fn parameter
  - TurnLoop context overflow path: replaced empty `continue` with emergency
    compaction via self._compact_fn
  - ~40 LOC across engine.py + turn_loop.py

[DONE] Step 3: Wired capacity into Engine with 3 checkpoints.
  - Created src/deepseek_tui/engine/capacity_flow.py (~140 LOC):
    build_observation(), run_pre_request_checkpoint(),
    run_post_tool_checkpoint(), run_error_escalation_checkpoint()
  - Engine.__init__: instantiates CapacityController
  - Engine._run_conversation: calls all 3 checkpoints at correct points
  - Error counting: step_error_count and consecutive_tool_error_steps tracked
  - Full VERIFY_WITH_TOOL_REPLAY and VERIFY_AND_REPLAN deferred (P1)

[DONE] Step 4: Wired command_safety into ExecShellTool heuristic fallback.
  - Replaced `lambda _: Decision.ALLOW` with _command_safety_heuristic()
  - _command_safety_heuristic maps SafetyLevel -> Decision:
    SAFE/WORKSPACE_SAFE -> ALLOW, REQUIRES_APPROVAL -> PROMPT,
    DANGEROUS -> FORBIDDEN
  - ~10 LOC in shell_tools.py

[DONE] Step 5: Integration tests — 15 tests in
  tests/parity/phase_b/test_integration_wiring.py:
  - TestToolParserTurnLoopFallback (2 tests): text tool call detected,
    no false positive on plain text
  - TestCompactionWiring (3 tests): engine has config, short conv no compact,
    turn_loop accepts compact_fn
  - TestCapacityWiring (4 tests): engine has controller, 3 checkpoint calls
  - TestCommandSafetyHeuristic (6 tests): safe/workspace/dangerous/unknown
    mappings, policy integration via heuristic fallback

--- Issues Found ---

[BUG] AssistantResponseBuffer has no `.text` attribute — uses `.text_parts`
  (list[str]). The tool_parser integration initially accessed `buffer.text`
  which doesn't exist. Fixed by using `"".join(buffer.text_parts)` and
  `buffer.text_parts[:] = [parsed.clean_text]`.

[ISSUE] venv had to be rebuilt from scratch (Python 3.12.13 ensurepip fails
  in sandbox). Used --without-pip + bootstrap pip workaround.

--- Deferred Items ---

[DEFERRED] capacity_flow: VERIFY_WITH_TOOL_REPLAY (re-running read-only
  tools for verification) — full implementation requires tool registry
  introspection at checkpoint time. Logged decision, P1 follow-up.

[DEFERRED] capacity_flow: VERIFY_AND_REPLAN (canonical state rebuild) —
  full implementation requires CanonicalState struct and prompt builder.
  Logged decision, P1 follow-up.

========================================================================
2026-05-08  Engine Core Modules (context/dispatch/tool_execution/tool_catalog)
========================================================================

Goal: Complete the Engine's tool dispatch chain by creating/enriching 4 engine
modules mirroring Rust's context.rs, dispatch.rs, tool_execution.rs, and
tool_catalog.rs. Integrate them into engine.py.

--- Work Log ---

[DONE] Step 1: Enriched engine/context.py (~200 LOC)
  - Fixed TURN_MAX_OUTPUT_TOKENS to 262,144 (was 4096)
  - Added CONTEXT_HEADROOM_TOKENS, tool result size limit constants
  - Implemented summarize_text, summarize_text_head_tail for truncation
  - Added tool result compaction: _tool_result_is_noisy, compact_tool_result_for_context
  - Sub-agent result compaction: _compact_subagent_tool_result_for_context
  - System prompt management: extract_compaction_summary_prompt,
    remove_working_set_summary, append_working_set_summary
  - Token estimation: estimate_input_tokens_conservative, context_input_budget
  - Context error detection: is_context_length_error_message

[DONE] Step 2: Created engine/dispatch.py (~250 LOC)
  - ToolExecutionPlan, ToolExecOutcome, ParallelToolResult dataclasses
  - caller_type_for_tool_use, caller_allowed_for_tool for caller policy
  - format_tool_error for human-friendly error messages
  - parse_tool_input with 4 fallback strategies (JSON, code-fence strip,
    double-encoded, balanced-segment extraction)
  - parse_parallel_tool_calls for multi_tool_use.parallel payloads
  - Policy predicates: should_parallelize_tool_batch, should_stop_after_plan_tool,
    should_force_update_plan_first
  - MCP tool policy: mcp_tool_is_parallel_safe, mcp_tool_is_read_only

[DONE] Step 3: Created engine/tool_execution.py (~80 LOC)
  - emit_tool_audit: JSONL audit logging to $DEEPSEEK_TOOL_AUDIT_LOG
  - execute_tool_with_lock: asyncio.Lock for writes, concurrent for reads
  - _is_mcp_tool, _execute_mcp_tool for MCP tool dispatch

[DONE] Step 4: Created engine/tool_catalog.py (~500 LOC, replaces tool_setup.py)
  - Deferred loading policy: should_default_defer_tool, apply_native_tool_deferral,
    apply_mcp_tool_deferral, build_model_tool_catalog
  - Advanced tool injection: ensure_advanced_tooling (code_execution, tool_search)
  - Active tool set: initial_active_tools, active_tools_for_step,
    maybe_activate_requested_deferred_tool
  - Tool search: discover_tools_with_regex, discover_tools_with_bm25_like
  - Edit distance & suggestions: edit_distance, suggest_tool_names,
    missing_tool_error_message
  - Built-in executors: execute_tool_search, execute_code_execution_tool

[DONE] Step 5: Integrated into engine.py (~100 LOC changes)
  - _execute_tool_calls now passes model for context-aware tool result compaction
  - Extracted _execute_single_tool for special tool routing (tool_search, code_execution)
  - Extracted _handle_approval_flow for cleaner approval gate
  - Added emit_tool_audit calls for audit logging
  - Added format_tool_error for structured error messages
  - Added _tool_write_lock (asyncio.Lock) for future parallel dispatch
  - Missing tool now returns suggestions via missing_tool_error_message

[DONE] Step 6: Tests — 55 tests in test_engine_core_modules.py
  - TestSummarizeText (4): short/long text truncation
  - TestContextConstants (3): TURN_MAX_OUTPUT_TOKENS, headroom
  - TestContextBudget (2): budget calculation, exhaustion
  - TestCompactToolResult (3): empty, short, large noisy compaction
  - TestSystemPromptManagement (4): compaction summary, working set
  - TestContextLengthError (2): detection of context errors
  - TestTokenEstimation (1): positive token estimates
  - TestParseToolInput (5): JSON, code fences, double-encoded, prefix text
  - TestParseParallelToolCalls (2): valid parse, empty raises
  - TestCallerPolicy (3): direct/non-direct/default
  - TestFormatToolError (2): timeout, generic
  - TestDispatchPolicy (4): parallelize, stop-after-plan, force-plan
  - TestEmitToolAudit (2): no-op without env, JSONL write
  - TestToolDeferral (4): always-active, shell, unknown, yolo
  - TestToolSearch (3): regex, BM25, invalid regex
  - TestEditDistance (3): identical, single edit, empty
  - TestSuggestToolNames (2): similar, unrelated
  - TestMissingToolMessage (2): with/without suggestions
  - TestIsToolSearchTool (1): identification
  - TestExecuteToolSearch (3): regex activate, BM25 activate, missing query

[DONE] Step 7: Updated test_turn_loop.py tests to match new tool_catalog API
  - Tests now check for code_execution/tool_search instead of update_plan/note
  - Import path updated from tool_setup to tool_catalog

--- Verification ---

  ruff check:  ALL PASSED (0 errors)
  mypy:        ALL PASSED (0 errors)
  pytest:      578 passed, 0 failed (parity phase_a + phase_b + phase_c)

--- Issues Found ---

[ISSUE] tool_setup.py still exists as legacy file. Tests and imports all
  updated to tool_catalog.py. tool_setup.py can be removed when convenient.

--- Deferred Items ---

[DEFERRED] Parallel tool dispatch in engine.py: _tool_write_lock created but
  full parallel execution (asyncio.gather for read-only tools) not yet wired.
  The infrastructure (dispatch.should_parallelize_tool_batch, execute_tool_with_lock)
  is ready. P1 follow-up.

[DEFERRED] MCP tool dispatch via engine: _execute_mcp_tool in tool_execution.py
  implemented but engine.py doesn't yet route mcp__ prefixed tools through it
  (still relies on registry). P2 follow-up.
