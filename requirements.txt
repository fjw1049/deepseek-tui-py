# Runtime dependencies for deepseek-tui-py
# Floor versions only — for an exact-version reproducible install use
# requirements.lock (generated via `uv pip freeze` after a clean install).
#
# Install: pip install -r requirements.txt
#         (or: uv pip install -r requirements.txt)

textual>=0.50.0
httpx>=0.26.0
httpx-sse>=0.4.0
typer>=0.9.0
pydantic>=2.5.0
tomli>=2.0.1; python_version < "3.11"
aiosqlite>=0.19.0
keyring>=24.3.0
anyio>=4.2.0
rich>=13.7.0
jsonschema>=4.20.0
aiofiles>=23.2.1
fastapi>=0.110.0
uvicorn>=0.27.0
