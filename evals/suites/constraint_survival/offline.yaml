schema_version: 1
id: constraints.two_compactions_preserve_identity_and_state
suite: constraint_survival
risk: critical
runner: compaction_state
grader: constraints
tags: [compaction, request-ledger, structured-state]
related_paths:
  - src/deepseek_tui/engine/capacity.py
  - src/deepseek_tui/engine/context_pressure.py
  - src/deepseek_tui/engine/cycle.py
input:
  user_requests:
    - 不要修改 config.toml。
    - 修改完成后必须运行 test_auth.py。
    - 测试目前还没有运行，不要声称测试通过。
  working_set:
    - src/deepseek_tui/engine/prompts.py
  first_summary: |-
    ### Goal
    Continue the prompt audit.
    ### Next step
    Verify the tool boundary.
  second_summary: |-
    ### Goal
    Continue the prompt audit; previous claims are unverified.
    ### Next step
    Re-read current files and run the required test.
  structured_state:
    mode_label: plan
    workspace: /workspace/project
    cwd: /workspace/project/src
    todo_snapshot:
      - content: 完成权限回归
        status: in_progress
    plan_snapshot:
      - step: 运行 test_auth.py
        status: pending
    subagent_snapshots:
      - agent_id: agent-1
        role: reviewer
        objective: 审核权限边界
  structured_state_contains:
    - plan
    - /workspace/project/src
    - 完成权限回归
    - 运行 test_auth.py
    - agent-1
expect: {}
